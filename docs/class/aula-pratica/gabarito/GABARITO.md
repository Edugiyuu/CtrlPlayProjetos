<!--
  TEMPLATE — GABARITO. Arquivo do PROFESSOR. Nunca vai pro aluno.

  REGRA: mostre o ARQUIVO INTEIRO de como tem que ficar, etapa por etapa.
  Nada de fragmento solto — na hora da aula você não quer ficar montando
  o quebra-cabeça mentalmente. Diga sempre QUAL arquivo mudou e QUAL não.

  Apague este comentário depois de preencher.
-->

# Gabarito do DESAFIO.md — para o professor

Cada parte abaixo mostra **os arquivos inteiros**, no estado em que devem
ficar ao terminar aquela parte. As partes são cumulativas: a Parte 2 já inclui
o que foi feito na Parte 1.

Ponto de partida (fim da aula, antes do desafio): <onde está o código base —
ex.: "os 3 arquivos em `gabarito/src/`">.

---

## Parte 1 — <título igual ao do DESAFIO.md>

**Só o `<arquivo>` muda.** `<outro>` e `<outro>` ficam intactos.

<Se o aluno inventa o conteúdo (um nome, um produto), diga: "o exemplo abaixo
usa X; o que importa é <o critério real de correção>".>

### `<caminho/arquivo>`

```<linguagem>
<ARQUIVO INTEIRO>
```

### `<caminho/outro-arquivo>` (não mudou — confira que o aluno não mexeu aqui)

```<linguagem>
<ARQUIVO INTEIRO>
```

**Resposta esperada à pergunta:** <a explicação que você quer ouvir dele, no
nível dele. Se ele disser outra coisa, é aqui que você percebe o buraco.>

---

## Parte 2 — <título>

**Só o `<arquivo>` muda.**

<Se houver mais de uma solução válida, mostre as duas como "Forma A" e
"Forma B" — arquivo inteiro em cada uma — e diga que ambas contam.>

### `<caminho/arquivo>`

```<linguagem>
<ARQUIVO INTEIRO>
```

`<outros arquivos>` seguem iguais aos da Parte 1.

**Resposta esperada à pergunta:** <...>

---

## Parte 3 — <título>

**Só o `<arquivo>` muda.**

### `<caminho/arquivo>`

```<linguagem>
<ARQUIVO INTEIRO>
```

**Resposta esperada à pergunta:** <...>

---

## Estado final de todos os arquivos (fim das partes)

<Repita aqui TODOS os arquivos completos no estado final. Redundante de
propósito: é o que você abre na hora de conferir o trabalho do aluno.>

### `<caminho/arquivo-1>`

```<linguagem>
<ARQUIVO INTEIRO>
```

### `<caminho/arquivo-2>`

```<linguagem>
<ARQUIVO INTEIRO>
```

---

## Gabarito do "Se sobrar tempo"

### 1. <título do extra> — `<arquivo>`

```<linguagem>
<ARQUIVO INTEIRO>
```

### 2. <título do extra> — `<arquivo>`

```<linguagem>
<ARQUIVO INTEIRO>
```

### 3. <título do extra> — `<arquivo>`

```<linguagem>
<ARQUIVO INTEIRO>
```
